$passoptions.latex()$
\documentclass[
$for(babel-otherlangs)$
  $babel-otherlangs$,
$endfor$
$if(babel-lang)$
  $babel-lang$,
$endif$
$if(fontsize)$
  $fontsize$,
$endif$
$if(papersize)$
  $papersize$paper,
$endif$
$for(classoption)$
  $classoption$$sep$,
$endfor$
]{$documentclass$}
$if(beamerarticle)$
\usepackage{beamerarticle} % needs to be loaded first
$endif$
\usepackage{xcolor}
$if(geometry)$
\usepackage[$for(geometry)$$geometry$$sep$,$endfor$]{geometry}
$endif$
\usepackage{amsmath,amssymb}
$if(cancel)$
\usepackage{cancel}
$endif$
$--
$-- section numbering
$--
$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
$endif$
$fonts.latex()$
$font-settings.latex()$
$common.latex()$
$for(header-includes)$
$header-includes$
$endfor$
$after-header-includes.latex()$
$hypersetup.latex()$
$if(pdf-trailer-id)$

\ifXeTeX
\special{pdf:trailerid [ $pdf-trailer-id$ ]}
\fi
\ifPDFTeX
\pdftrailerid{}
\pdftrailer{/ID [ $pdf-trailer-id$ ]}
\fi
\ifLuaTeX
\pdfvariable trailerid {[ $pdf-trailer-id$ ]}
\fi
$endif$

% Packages de base
\usepackage[table]{xcolor}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage[french]{babel}
\usepackage{fancyhdr}
\usepackage{float}

% AJOUT: Packages pour les citations spéciales
\usepackage{mdframed}
\usepackage{etoolbox}
\usepackage{ifthen}

% Configuration
\geometry{
  left=2.5cm,
  right=2.5cm,
  top=2.5cm,
  bottom=2.5cm
}

% Couleurs
\definecolor{maritimeBlue}{RGB}{0, 80, 120}
\definecolor{maritimeLightBlue}{RGB}{173, 216, 230}

% AJOUT: Styles pour les citations spéciales
\definecolor{notebg}{RGB}{232, 244, 255}    % Bleu très clair pour les notes
\definecolor{noteborder}{RGB}{33, 150, 243} % Bleu pour la bordure
\definecolor{warningbg}{RGB}{255, 248, 225} % Jaune clair pour les avertissements
\definecolor{warningborder}{RGB}{255, 193, 7} % Jaune pour la bordure

% Environnements pour les citations spéciales
\newmdenv[
  backgroundcolor=notebg,
  linecolor=noteborder,
  linewidth=2pt,
  leftmargin=0pt,
  rightmargin=0pt,
  innerleftmargin=10pt,
  innerrightmargin=10pt,
  innertopmargin=8pt,
  innerbottommargin=8pt,
  skipabove=10pt,
  skipbelow=10pt,
  font=\itshape
]{notebox}

\newmdenv[
  backgroundcolor=warningbg,
  linecolor=warningborder,
  linewidth=2pt,
  leftmargin=0pt,
  rightmargin=0pt,
  innerleftmargin=10pt,
  innerrightmargin=10pt,
  innertopmargin=8pt,
  innerbottommargin=8pt,
  skipabove=10pt,
  skipbelow=10pt,
  font=\itshape
]{warningbox}

% Commande pour les notes spéciales
\newcommand{\specialnote}[1]{%
  \begin{notebox}
  #1
  \end{notebox}
}

\newcommand{\specialwarning}[1]{%
  \begin{warningbox}
  #1
  \end{warningbox}
}

% pour les cours
\newenvironment{propriete}[1][]
  {\par\medskip\noindent\textbf{Propriété #1.}\ \ignorespaces}
  {\par\medskip}

\newenvironment{definition}[1][]
  {\par\medskip\noindent\textbf{Définition #1.}\ \ignorespaces}
  {\par\medskip}

\newenvironment{remarque}
  {\par\medskip\noindent\textbf{Remarque.}\ \ignorespaces}
  {\par\medskip}

\newenvironment{theoreme}[1][]
  {\par\medskip\noindent\textbf{Théorème #1.}\ \ignorespaces}
  {\par\medskip}

% Dans le préambule de ton document LaTeX
\newenvironment{cadre}
  {\par\medskip\noindent\begin{center}\begin{tabular}{|p{\dimexpr\textwidth-4pt}|}\hline\ignorespaces}
  {\\\hline\end{tabular}\end{center}\par\medskip}


% En-têtes et pieds de page
\pagestyle{fancy}
\fancyhf{}

\fancyhead[L]{\textcolor{maritimeBlue}{\nouppercase{\leftmark}}}
\fancyhead[R]{\colorbox{maritimeBlue}{\textcolor{white}{Tpro}}}
\fancyfoot[L]{\footnotesize\textcolor{maritimeBlue}{Document mis à jour le \today}}
\fancyfoot[C]{\colorbox{maritimeBlue}{\textcolor{white}{\thepage}}}
\fancyfoot[R]{\footnotesize\textcolor{maritimeBlue}{LPM BL-ZEG}}

% Configuration des couleurs des lignes du tableau
\rowcolors{2}{maritimeLightBlue!30}{white}

% Désactive les figures flottantes
\floatplacement{figure}{H}

$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$
$if(subtitle)$
\usepackage{etoolbox}
\makeatletter
\providecommand{\subtitle}[1]{% add subtitle to \maketitle
  \apptocmd{\@title}{\par {\large #1 \par}}{}{}
}
\makeatother
\subtitle{$subtitle$}
$endif$

\author{$for(author)$$author$$sep$ \and $endfor$}
\date{$date$}




% Redéfinition de l'environnement quote pour les citations spéciales
\usepackage{mdframed}
\usepackage{etoolbox}

% Styles pour les citations
\definecolor{quotebg}{RGB}{240, 248, 255}    % Bleu très clair
\definecolor{quoteborder}{RGB}{100, 149, 237} % Bleu cornflower

% Environnement quote redéfini
\let\oldquote\quote
\let\endoldquote\endquote

\renewenvironment{quote}{%
  \begin{mdframed}[
    backgroundcolor=quotebg,
    linecolor=quoteborder,
    linewidth=0pt,
    leftmargin=0pt,
    rightmargin=0pt,
    innerleftmargin=10pt,
    innerrightmargin=10pt,
    innertopmargin=8pt,
    innerbottommargin=8pt,
    skipabove=10pt,
    skipbelow=10pt,
    font=\itshape
  ]%
}{%
  \end{mdframed}%
}











\begin{document}

$if(has-frontmatter)$
\frontmatter
$endif$
$if(title)$
%\maketitle
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$
$endif$

$for(include-before)$
$include-before$

$endfor$
$if(toc)$
$if(toc-title)$
\renewcommand*\contentsname{$toc-title$}
$endif$
{
$if(colorlinks)$
$if(toccolor)$
\hypersetup{linkcolor=$toccolor$}
$endif$
$endif$
\setcounter{tocdepth}{$toc-depth$}
\tableofcontents
}
$endif$
$if(lof)$
\listoffigures
$endif$
$if(lot)$
\listoftables
$endif$
$if(linestretch)$
\setstretch{$linestretch$}
$endif$
$if(has-frontmatter)$
\mainmatter
$endif$
$body$

$if(has-frontmatter)$
\backmatter
$endif$
$if(nocite-ids)$
\nocite{$for(nocite-ids)$$it$$sep$, $endfor$}
$endif$
$if(natbib)$
$if(bibliography)$
$if(biblio-title)$
$if(has-chapters)$
\renewcommand\bibname{$biblio-title$}
$else$
\renewcommand\refname{$biblio-title$}
$endif$
$endif$
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}

$endif$
$endif$
$if(biblatex)$
\printbibliography$if(biblio-title)$[title=$biblio-title$]$endif$

$endif$
$for(include-after)$
$include-after$

$endfor$
\end{document}
